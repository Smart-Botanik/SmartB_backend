/**
 * @deprecated BK-MS-REFDATA-3 — Brand/Product live in reference-data-service.
 * Seed: `cd services/reference-data && npm run db:seed`
 */
console.error(
  "create-sample-data.ts is deprecated. Use: cd services/reference-data && npm run db:seed",
);
process.exit(1);
